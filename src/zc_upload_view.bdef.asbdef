projection;
strict ( 1 );
use draft;

define behavior for ZC_UPLOAD_VIEW alias uploadProj
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}